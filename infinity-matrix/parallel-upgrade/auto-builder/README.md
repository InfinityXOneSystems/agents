Auto Builder (scaffold)
=======================

This is a minimal Auto Builder service scaffold. It accepts text prompts and returns a mock plan or code snippet.

Run locally:

```bash
cd parallel-upgrade/auto-builder
npm install
npm start
```
