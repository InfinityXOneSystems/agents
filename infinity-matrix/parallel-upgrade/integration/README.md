# Integration helper

Small client to call the Ollama HTTP adapter. Install requirements and run:

```bash
python -m venv .venv
. .venv/bin/activate
pip install -r requirements.txt
python llm_client.py philschmid/phi-3 "Summarize: The quick brown fox"
```
