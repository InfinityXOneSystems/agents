# parallel-upgrade workspace

This folder is a non-invasive workspace for experimenting with adapters and integration code without modifying `infinity-matrix` directly.

Subfolders:
- `ollama-adapter`: FastAPI adapter that proxies to your local `ollama` CLI.
